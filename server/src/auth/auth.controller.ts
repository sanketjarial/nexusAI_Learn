import { Controller, Get } from '@nestjs/common';

/**
 * Controller handling authentication-related HTTP requests.
 * @class
 * @description Provides endpoints for user authentication, including login,
 * registration, password reset, and token management. This controller is
 * responsible for securing user access to the application.
 */
@Controller('auth')
export class AuthController {
}
